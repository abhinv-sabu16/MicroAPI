{
  "name": "@repo/user-service",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "User CRUD microservice with Postgres/Prisma — full implementation Day 6",
  "main": "dist/index.js",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit",
    "test": "echo \"Tests added Day 6\"",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@repo/shared": "workspace:*",
    "fastify": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.4.0",
    "tsx": "^4.7.1"
  }
}
